# v0.1.45 (2026-01-05)

## Added

- `amanmcp init` now generates `.amanmcp.yaml` template with documented configuration options
- `amanmcp init --config-only` flag to fix config without reindexing
- `amanmcp init` now adds usage guide to CLAUDE.md for intelligent Claude Code tool usage
  - Creates CLAUDE.md if not present
  - Appends guide section if CLAUDE.md exists without guide
  - Idempotent: marker-based detection prevents duplicate sections

## Changed

- `amanmcp init` now always uses `.mcp.json` for project scope to ensure `cwd` field is set (BUG-041)

## Fixed

- BUG-040: Add missing `type` field to `.mcp.json` configuration (default: "stdio")
- BUG-041: Use `.mcp.json` for project scope to ensure `cwd` is set correctly
- BUG-042: Validate existing `.mcp.json` configuration and warn about missing required fields

## Documentation

- Add bug documentation for BUG-040, BUG-041, BUG-042
- Expand README CLI commands section (5 → 13 commands with flags)
- Add MCP tools section to README explaining search, search_code, search_docs, index_status
- Document init command flags in first-time-user-guide.md
- Add daemon, compact, sessions commands to evaluation guide

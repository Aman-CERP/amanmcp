# Version 0.1.20

**Release Date:** 2025-12-31
**Phase:** Phase 3 - Polish & Release (29%)

---

## Summary

Implemented Hugot as the default embedding provider, achieving "It Just Works" - no Ollama service required for default operation. Models auto-download from HuggingFace on first use.

---

## Added

- F25a: Hugot Embedder - zero-config embedding provider
  - HugotEmbedder using knights-analytics/hugot library
  - Embedder factory with fallback chain: Hugot → Ollama → Static
  - Default model: all-MiniLM-L6-v2 (384 dims, 512 context)
  - EmbeddingGemma support via `--model=embeddinggemma` (768 dims, 2048 context)
  - Auto-download from HuggingFace on first use

---

## Changed

- Default embedding provider changed from "ollama" to "hugot"
- Default embedding model changed to "minilm" (384 dimensions)
- CLI commands (index, search, serve) now use embedder factory pattern

---

## Technical Details

### New Files

| File | Purpose |
|------|---------|
| `internal/embed/hugot.go` | HugotEmbedder implementation |
| `internal/embed/factory.go` | Embedder factory with fallback chain |
| `internal/embed/hugot_test.go` | Comprehensive tests |

### Modified Files

| File | Changes |
|------|---------|
| `internal/config/config.go` | Default provider: hugot, model: minilm, dims: 384 |
| `cmd/amanmcp/cmd/index.go` | Factory pattern for embedder |
| `cmd/amanmcp/cmd/search.go` | Factory pattern for embedder |
| `cmd/amanmcp/cmd/serve.go` | Factory pattern for embedder |
| `docs/decisions/ADR-005-hugot-embedder.md` | Implementation notes |

### Quality Metrics

| Metric | Value |
|--------|-------|
| Tests | All passing |
| Coverage | 63.7% (embed), 79.5% overall |
| CI Check | PASS |

---

## Usage

```bash
# Default (Hugot + MiniLM - auto-downloads, no external deps)
amanmcp index .

# Power user (Ollama - legacy)
amanmcp index . --embedder=ollama

# Offline (Static - always works)
amanmcp index . --offline
```

---

## What's Next

- Validate F25a Hugot Embedder
- Continue with F21 (Progress UI) or F22 (Error Handling)

---

*Phase 3: 29% complete (F19, F20, F25a)*

# v0.1.42 (2026-01-05)

## Changed

- **BREAKING**: Default embedder changed from Static768 to Ollama API (qwen3-embedding:8b)
  - Ollama must be running for optimal semantic search quality
  - Falls back to Static768 automatically if Ollama unavailable
  - Set `AMANMCP_EMBEDDER=static` to force static embedder

## Fixed

- Fix golangci-lint v2 schema validation (move exclude-rules to linters.exclusions)
- BUG-021: Resolved - Migrated from llama.cpp (SIGBUS crashes) to Ollama HTTP API
